#include <iostream>

void insertionSort(int arr[], int n)
{
	for (size_t i = 1; i < n; i++)
	{
		//pick an element that will be inserted
		int key = arr[i];

		//start comprating it with the previous one
		int j = i-1;

		//iterate until the start is reached
		//and we have elements which are bigger
		while (j >= 0 && arr[j]>key)
		{
			//then we swap the bigger element with our key
			arr[i] = arr[j];
			arr[j] = key;
			//continue with the previous element
			j--;
		}
	}
}

int main()
{
	int count;
	std::cin >> count;

	int *arr = new int[count];

	for (size_t i = 0; i < count; i++)
	{
		std::cin >> arr[i];
	}
	insertionSort(arr, count);
	for (size_t i = 0; i < count; i++)
	{
		std::cout << arr[i] << " ";
	}

	system("pause");
	return 0;
}
